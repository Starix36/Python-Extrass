bal = 1000
minbal=500
withdraw = int(raw_input("Enter withdraw amount"))
if bal < withdraw:
    print "Cannot Withdraw"
else:
    if minbal < bal - withdraw:
        print "You can withdraw"
    else:
        print "Maintain Balance"
    
    
